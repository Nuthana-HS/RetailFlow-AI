# =============================================================================
# RetailFlow AI — Backend Dev Dependencies
# =============================================================================
# Install with: pip install -r requirements-dev.txt
# This includes production deps + testing + linting tools
# Audited: 2026-07-02
#
# Changes vs. original:
#   REMOVED types-python-dateutil — python-dateutil removed from production deps
#   REMOVED types-pytz            — pytz removed from production deps
#   UPDATED package versions to match those installed in the project venv
# =============================================================================

# Include production dependencies
-r requirements.txt

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==9.1.1                      # [installed]
pytest-asyncio==1.4.0              # [installed]
pytest-cov==7.1.0                  # [installed]
pytest-mock==3.14.0                # [stable]  — mock fixtures
httpx==0.28.1                      # [installed] — async HTTP client for test requests
asgi-lifespan==2.1.0               # [stable]   — ASGI lifespan management in tests
factory-boy==3.3.1                 # [stable]   — test data factories
faker==30.3.0                      # [stable]   — realistic fake data

# ---------------------------------------------------------------------------
# Linting & Type Checking
# ---------------------------------------------------------------------------
ruff==0.7.0                        # [stable]  — fast Python linter + formatter
mypy==1.12.0                       # [stable]  — static type checker

# Type stubs
types-passlib==1.7.7.20240819      # [stable]  — passlib type hints

# ---------------------------------------------------------------------------
# Development Utilities
# ---------------------------------------------------------------------------
ipython==8.28.0                    # [stable]  — enhanced Python REPL
watchdog==5.0.3                    # [stable]  — file watching (uvicorn --reload)

# ---------------------------------------------------------------------------
# Database Testing
# ---------------------------------------------------------------------------
aiosqlite==0.22.1                  # [installed] — SQLite async driver for unit tests
